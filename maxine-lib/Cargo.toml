[package]
name = "maxine-lib"
version = "0.1.0"
edition = "2021"

[package.metadata.nix]
build = true

[dev-dependencies]
criterion = "0.4"

[[bench]]
name = "transforms"
harness = false

[dependencies]
nalgebra = "0.31.3"
plotters = { version = "0.3.4", optional = true }
